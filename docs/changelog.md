# 变更记录

## 2026-06-14

- 继续推进第一批维护性优化：`calculator.py` 收缩为兼容导出入口，单 ETF 最新/日度指标迁入 `etf_metrics.py`，窗口级样本过滤、分组累计、覆盖率和质量 payload 迁入 `etf_window.py`；现有服务和测试 import 路径保持兼容，计算口径不变。
- AI 摘要候选信号评分迁入 `ai_summary_signals.py`，`ai_summary.py` 聚焦缓存、模型调用、解析和规则摘要；后续调整“哪些资金信号值得 DeepSeek 关注”可独立迭代。
- `scripts/audit_capital_flow_snapshot.py` 默认将审计 JSON 落盘到 `logs/audits/capital_flow_snapshot/`，保留每日数据质量、分类覆盖率和异常行记录；支持 `--no-write-log` 临时只输出。
- 修复前端入口重复初始化问题：`capital_flow.js` 改为 DOM ready 后只执行一次 `setupSectionNav()` 和 `loadCapitalFlow()`，避免重复请求、半初始化状态和页面偶发加载失败；UI 契约测试新增重复初始化保护。
- 新增页面资源烟测 `scripts/verify_dashboard_page.py`，验证首页实际下发的 6 个拆分 JS、脚本顺序、入口单次初始化、静态资源可访问和 `/api/capital-flow` 可渲染，用于捕捉“页面只有标题/加载失败”类问题。
- 继续优化长期维护性：服务层 `_build_capital_flow_payload` 拆出市场输入准备、NAV/复权因子加载、多窗口 payload 组装和口径说明；AI 总结 prompt 拆到 `ai_summary_prompt.py`，以后调提示词不再混入缓存/解析逻辑。
- 新增后台资金流快照审计 `scripts/audit_capital_flow_snapshot.py`，读取最近成功 payload 缓存并提示 stale 缓存、NAV 估算占比、跳过流量点、分类覆盖率和大额/高占比/高成交均值占比异常行；新增 `observability.py` 输出 payload 缓存/构建结构化日志。`verify_all.sh` 接入分类主数据校验。
- 加固资金流向缓存兜底状态：当主数据刷新失败或服务 warm start 使用上次成功 payload 时，`payload_cache_status=stale` 和错误原因会同步写入顶层、当前窗口和全部 `window_payloads` 的 ETF `data_status`，避免后台审计或 AI 摘要误以为所有窗口都是新鲜计算结果。
- AI 摘要输入新增 `quality.payload_cache_status` / `payload_cache_error`，并更新 DeepSeek prompt：缓存兜底数据只能按“上次成功数据”解读，不能表述为本次已成功刷新的最新结果。
- 修正 ETF 明细排错字段的多日窗口口径：`top_etfs/debug_etfs` 中 `previous_share_wan`、`share_change_wan` 现在使用窗口期初可比份额，并新增 `window_start_share_wan`、`window_share_change_wan`，避免排查长窗口异常时误读为最新相邻两日份额差。
- 新生成的资金流 payload 增加 `payload_schema_version`，用于后续排查磁盘缓存是否为旧结构；旧缓存仍允许作为兜底读取，避免首次重算过慢导致页面空白。
- 优化 ETF 分类覆盖率口径：API 覆盖率改为“已分类前台目标 / 前台可聚合目标”，新增 `frontend_target_equity_etf_count`、`non_frontend_target_equity_etf_count` 和 `raw_target_coverage_pct`，指数增强等非前台目标不再混入真正未分类缺口。审计脚本同步输出 `non_frontend_target_samples`。
- 补充一批高确定性 ETF 精确分类主数据，包括创业板200、创业板综、深证主板50、A100、MSCI中国A50、民企、国企改革、港股国企、ESG、央企科创、信息安全、稀有金属、农牧等；期货型 ETF 按非权益目标排除。分类审计显示前台可聚合覆盖率提升至 83.59%，精确映射提升至 854，关键词兜底降至 231。
- 做一轮行为不变的可维护性拆分：`calculator.py` 收缩为 ETF 窗口主流程，分组/审计迁入 `grouping.py`，价格/拆分计算迁入 `price_math.py`，北上南下和格式化分别迁入 `north_south.py`、`formatting.py`。原 1541 行 `test_capital_flow_service.py` 拆为 service、taxonomy、calculator 三个测试文件。
- 新增 `scripts/validate_taxonomy_data.py` 校验 ETF 分类主数据，检查 JSON 结构、重复 benchmark、字段合法性，并可附带当前分类覆盖率审计。前端 JS 拆为 state、format、data、charts、table、入口六个模块，验证脚本改为检查全部 `capital_flow*.js`。

## 2026-06-13

- 优化本地验证和启动链路：新增 `scripts/lib_env.sh` 统一管理 `.venv`、requirements hash 和 pycache 路径；`start_web.sh`、`verify_all.sh` 复用同一环境逻辑，默认虚拟环境损坏时自动重建，依赖未变化时跳过 `pip install`。新增 `verify_fast.sh`、`check_web.sh`、`restart_web.sh`，降低日常测试、接口探活和服务重启成本。
- 优化 DeepSeek prompt 结构：将“挖掘最值得关注、最能辅助后续观察决策的 3-5 个信号”前置为主目标，筛选维度扩展为金额、净申购占比、涨跌幅、成交均值占比及其组合异常，并明确这些维度不要求全部满足；新增同一资金主线优先合并、避免重复关注点的要求。同步提升 `AI_SUMMARY_PROMPT_VERSION`，使旧 AI 摘要缓存自动失效。
- 新增 AI 摘要缓存：DeepSeek 成功摘要按“摘要输入哈希 + 模型 + API 地址 + prompt 版本”缓存 24 小时，同一数据版本反复打开页面不再重复请求；底层资金流数据、模型或 prompt 变化会自动失效，`refresh=1` 可人工强刷。
- 完善 DeepSeek 输入数据口径：候选信号新增同窗口复权涨跌幅和同窗口成交均值占比，字段改为 `flow_Xd_yi`、`change_Xd_pct`、`turnover_Xd_avg_pct`；新增 `metric_notes` 约束模型只有同窗口数据才能称为同期、共振或背离，避免把 60 日净申购和 1 日涨跌幅错误配对。AI 输出进一步统一为表格术语（净申购占比、成交均值占比），并要求亿元金额按表格精度表达，标题右侧仅保留模型名。
- 微调状态栏与 AI 总结：暖启动缓存提示改为普通灰色状态，不再使用橙色警告；AI 正文颜色加深。DeepSeek prompt 改为按“辅助决策价值”筛选关注点，优先挖掘异常、共振、背离、持续性冲突和风格切换，并要求说明资金行为含义和下一步观察点。
- 修复服务重启或首次请求慢导致页面像“只有标题”的问题：HTML 默认展示数据加载占位；主 payload 新增 12 小时磁盘暖启动缓存，服务重启后优先快速返回上次成功 payload 并在状态栏提示，避免等待 TuShare 重建缓存期间首屏空白。
- AI 总结关注点要求按重要性从高到低排序，后端规范化最多保留 5 条，避免摘要过长或重点顺序不稳定。
- 继续优化 AI 总结可读性：去掉编号和底部风险分区，正文放大并加深颜色，风险/谨慎解读点改由 DeepSeek 合并进对应关注点正文，减少重复信息和视觉干扰。
- 调整 AI 总结展示策略：主表加载阶段不再展示规则摘要，DeepSeek 未返回或失败时隐藏 AI 总结模块；DeepSeek 成功后才展示。前端关注点从横向卡片改为纵向列表，去除“数据日对齐 / NAV 估算占比”等数据质量文案展示，并放宽模型文案长度约束，强调输出关键关注点和解读理由。
- 拆分 AI 摘要加载链路并加固主表兜底：主表接口 `/api/capital-flow` 不再同步等待 DeepSeek，只返回核心数据和隐藏摘要占位；新增 `/api/capital-flow/ai-summary` 在页面渲染后单独调用 DeepSeek，默认超时 30 秒且可通过 `DEEPSEEK_TIMEOUT_SECONDS` 调整。成功生成的主 payload 会落盘缓存，TuShare 短时限流或网络失败时返回上次成功 payload 并在状态栏提示，避免模型或外部数据源问题导致整页表格空白。
- 优化资金流向计算层可维护性：新增 `policy.py` 集中窗口、阈值、缓存和份额拆分识别容差等口径参数；新增 `types.py` 承载计算层 TypedDict/dataclass；拆分 `calculator.py` 中 ETF 主循环的静态信息、最新日指标、日度指标、聚合累加和 top ETF 组装逻辑；测试不再通过 `service.py` 私有兼容别名引用底层函数。新增 `tests/fixtures/etf_flow_regression_snapshot.json` 固定普通净申购、成交占比和份额拆分样本的核心会计输出，确保本次为行为不变重构。
- 正式对齐 DeepSeek 当前 API 口径：AI 总结默认模型从即将废弃的 `deepseek-chat` 切换为 `deepseek-v4-flash`，请求显式关闭 thinking、启用 JSON 输出、限制输出长度并补充 JSON 示例；文档新增 DeepSeek 配置和“规则摘要”回退排查说明。
- 优化页面头部 AI 总结展示：关注点从“数据 + tag 胶囊”改为“标题 + 一到两句原因说明”，后端规范化时丢弃模型返回的 `tags`，前端移除标签渲染和样式，使摘要更像可直接阅读的决策提示。
- 新增页面头部 AI 总结：后端从资金流 payload 压缩出结构化摘要输入，覆盖 ETF 数据质量、各窗口分区净申购、Top 流入/流出和“一级净申购 + 二级成交热度 + 当日涨跌幅”候选信号；配置 `DEEPSEEK_API_KEY` 后调用 DeepSeek 生成固定 JSON 摘要，未配置或调用失败时使用本地规则摘要兜底。前端展示一句话总览、关键关注点、风险和数据质量，不改变任何计算口径。
- 修正目标权益 ETF 样本池判定顺序：精确 taxonomy 主数据中明确标为权益的指数先于泛化排除词生效，避免“标普中国A股红利100”“标普港股通低波红利”等本应纳入前台分类的 ETF 被 `标普` 海外排除词挡在严格数据对齐和 NAV 回填集合之外；普通标普海外 ETF 仍按非目标排除。
- 收紧 ETF 聚合样本池：计算层先过滤非目标 ETF，再执行行业/策略聚合，避免“标普500消费精选”等海外 ETF 被消费关键词兜底后误入 A 股行业；补充“标普中国A股红利机会指数”精确主数据，保留对应 A 股红利 ETF。
- 加固 ETF 单位净值覆盖：`fund_nav` 先按交易日批量读取，若目标权益 ETF 当日有价格但缺净值，再按单只 ETF 拉取窗口历史净值回填；仍缺失才使用收盘价估算。API 数据状态新增 `nav_backfilled_count`，用于观察本轮补齐的 NAV 日度点数量，降低批量接口覆盖不完整对净申购金额、ETF 规模和占比分母的影响。
- 修复 TuShare 文件缓存中带 ETF 代码 key 的路径生成问题：不再把 `510300.SH_...` 的 `.SH_...` 误识别为文件后缀并截断，避免不同市场代码或不同历史查询区间落到同一个缓存文件；缓存写入改用唯一临时文件，降低并发刷新时 `.tmp` 文件互相覆盖的风险。
- 优化页面金额展示精度：金额仍以亿元为单位，绝对值达到 100 亿元及以上展示为整数，10-99 亿元展示 1 位小数，10 亿元以下展示 2 位小数；该调整只影响前端展示，不改变 API 原始数值和计算口径。
- 加固 ETF 数据日严格对齐口径：最新候选交易日中，目标权益 ETF 若在份额接口出现，或在价格接口出现且最近 3 个候选交易日内曾有份额序列，就纳入 100% 价格/份额完整性校验，避免极端数据缺口下误用“有价格但无最新份额”或“有份额但无价格”的当天数据；近期完全没有份额序列的 ETF 不阻断全页。
- 将 ETF 规模相关口径从收盘价优先调整为净值优先：当日 ETF 规模、窗口期初规模、净申购占比分母和成交均值占比分母优先使用 `fund_nav.unit_nav`，净值缺失时再用收盘价估算，更接近机构 AUM 口径；后台 `scale_audit` 仍独立使用收盘价市值口径做规模变化闭合校验。
- 加固 `fund_adj` 复权因子覆盖：交易日批量查询若因接口返回上限漏掉目标 ETF，会按单只 ETF 拉取窗口历史复权因子回填，避免新代码段 ETF 涨跌幅退回未复权收盘价口径。
- 全盘加固 ETF 事件边界口径：接入 TuShare `fund_adj` 复权因子计算当日和分天涨跌幅，避免现金分红、分拆、合并或折算造成假涨跌；份额折算识别从单向分拆扩展为双向分拆/合并，只在份额倍数变化和后续价格反向调整同时成立时调整净申购，防止把非申赎事件误计为资金流。
- 修复 ETF 份额分拆/折算污染净申购的问题：计算层新增份额倍数突变且后续价格反向调整的识别逻辑，按可比份额和可比价格计算分拆日净申购、分天涨跌幅权重、成交均值占比分母和规模归因审计；API 新增 `split_adjusted_count`，页面问号和数据口径文档同步说明该调整，避免将 `515050.SH` 这类分拆误计为百亿级资金流入。
- 优化资金流向计算口径：`5日成交均值占比` 从“窗口成交额日均值 / 窗口期初规模”调整为“逐日成交额 / 当日期初规模后取均值”，总览、明细表和 5 日滑动走势图同步使用日度分母，降低窗口内规模大幅变化对二级市场热度指标的扭曲。新增净申购估值状态字段，页面状态栏显示“净申购估值”口径；新增后台 `scale_audit` 规模归因审计，用于校验规模变化、净申购贡献和市场涨跌影响的残差。
- 名称列悬停提示改为按实际截断状态启用：只有指数/主题名称被省略号截断时才显示完整名称浮层，完整展示的名称不再弹出黑色提示。
- 指数/主题名称悬停提示从浏览器原生 `title` 改为自定义浮层，避免内嵌浏览器中悬停不显示或触发过慢；名称列仍保持省略号。
- 微调明细行展开后的滚动对齐：目标行与 sticky 表头底线轻微重叠，避免表头和数据行之间露出前一行或背景细缝。
- 优化明细表视觉细节：宽基模块大标题恢复为“宽基被动ETF”，20 亿阈值保留在问号口径说明和总览列；表格标题行明确取消左右竖线分隔，仅保留底部分隔线；指数/主题名称列继续省略过长文本，但悬停可查看完整名称和指数代码。
- 优化明细行展开定位：点击展开表格行时，页面会自动将该行平滑滚动到 sticky 表头下方，方便连续查看该行指标和下方 4 张平铺图；收起行时不触发滚动。
- 宽基被动 ETF 展示规则与行业/策略统一为 20 亿元规模阈值，过滤深证50、国证2000等当前聚合规模不足 20 亿元的噪音项；宽基总览列和模块标题同步标注 `>=20亿`。补齐深证50、国证2000、中证800、中小100的指数代码，保证宽基表格名称后括号代码展示一致。
- 明细表新增 sticky 表头：滚动查看中间行展开的 4 张平铺图时，列标题固定在页头和分区导航下方，便于对照指标；桌面宽度默认放开表格横向 overflow 以保证纵向 sticky 生效，较窄屏幕保留横向滚动。
- 压缩明细表宽度以适配 13.3 寸屏幕：第一列从 220px 收到 156px，数值列从 116px 收到 98px，表格最小宽度从 1496px 降到约 1234px，并对长指数名称使用省略号，减少横向滚动。
- 主表观察窗口从 `1日 / 3日 / 5日 / 20日 / 60日` 收敛为 `1日 / 5日 / 20日 / 60日`，保留日、周、月、季四档趋势观察，去掉信息增量较低的 `3日` 列；总览矩阵左侧窗口文案从 `X交易日` 改为 `近X个交易日`。
- 将前台指标短名从“5日成交额均值占比”统一缩短为“5日成交均值占比”；口径不变，仍为近 5 个交易日场内成交额日均值 / 窗口期初 ETF 规模。复核宽基聚合口径为全组成交额日均值 / 全组期初规模，等价于按期初规模加权后的成交活跃度，不是 ETF 简单平均。
- 优化展开图和表格说明：明细表列头不再逐列展示问号，统一迁移到各分区标题问号中说明表格口径；展开图高度和间距压缩，便于 13 寸屏幕同时查看 4 张图；“5日滑动窗口成交均值占比”为非负指标，图表去除 0 轴并让标题上下居中；图表 tooltip 改为单行展示并按图表宽度向左/向右夹紧，避免屏幕右侧悬停时变成长黑条。
- 将 ETF 二级市场热度指标从“5日成交额占比”调整为“5日成交均值占比”：`turnover_yi` 继续保留窗口累计成交额，`turnover_ratio` 改为窗口成交额日均值 / 窗口期初 ETF 规模；总览、明细表和展开图同步改名并按日均口径展示。
- 优化成交均值占比展示细节：总览矩阵净申购窗口文案改为 `X交易日`，并加宽首列、略降字号，避免“5日成交均值占比”换行。
- 新增 ETF 二级市场交易热度指标：`fund_daily` 缓存扩展为同次读取收盘价和成交额；后端新增 `turnover_yi`、`turnover_ratio`、`daily_turnover` 字段。总览矩阵首行展示“5日成交均值占比”，明细表在当日涨跌幅后新增同名列，展开图在分天涨跌幅下方新增“5日滑动窗口成交均值占比”。图表底部日期刻度已移除，左侧标题按各图 0 轴位置对齐。
- 展开图新增“分天涨跌幅”走势图：后台新增 `daily_change_pct` 序列，按对应交易日 ETF 收盘价相对上一 ETF 交易日收盘价计算单只涨跌幅，并用当日 ETF 规模加权聚合；前端将该图置于分天净申购金额和 5 日滑动净申购金额上方。
- 优化表格展开图表：展开区改为左对齐全宽展示，上下两张图分别展示“分天净申购金额”柱状图和“5日滑动窗口净申购金额”趋势折线；日度柱按净申购/净赎回使用绿色/红色区分，并改用跟随鼠标的自定义悬停层稳定展示日期、时间段和净申购金额。折线点加大，悬停时按实际柱心/折线点横坐标匹配最近数据点，避免鼠标向后移动时仍展示前几天数据。
- 修复展开图表在宽表格下的悬停日期偏移：SVG 默认保持宽高比会让真实图形在宽容器中水平居中，导致鼠标位置按容器全宽换算后偏早；现改为图形横向填满容器，确保柱子/折线点、底部日期和 tooltip 使用一致横坐标。
- 优化展开图表在宽表格中的可读性：图表内容改为 sticky 在横向滚动容器的可视区域内，按屏幕宽度铺满，不需要跟随表格横向滑动查看；每张图标题移到左侧标题列，宽度对齐表格第一列并垂直居中。
- 继续优化 A 股行业分类后台复核：申万 2021 暴露审计改为按当前 ETF 规模优先查询高影响指数，输出样本新增 `scale_yi` 和 `etf_count`；新增 `label_consistency` 标签一致性审计，用于发现同一前台标签下不同跟踪指数的申万 top 行业分歧。补充证券、通信设备、化工、创新药、银行、工业有色、机器人等已验证可用于成分暴露的高影响指数代码；医疗、电力公用、创业板人工智能等本轮未唯一确认的指数暂不补码。
- 新增 A 股 ETF 成分股行业暴露审计：`scripts/audit_etf_taxonomy.py --with-sw-exposure` 会基于 TuShare 指数基础信息、指数成分权重和申万 2021 一级行业成分，输出跟踪指数的前五大申万行业暴露、未解析指数代码、无权重数据和限额未检查样本；指数代码解析按中证、上证、深证/国证做发布方兼容过滤，避免短名称跨体系误配。该能力仅用于后台复核主题/行业边界，不自动改变前台分类。
- 将 ETF 精确分类映射迁入 `src/capital_flow/taxonomy_data.json` 主数据文件，分类运行时优先读取主数据；`taxonomy.py` 只负责指数名称归一化、分类优先级和关键词兜底。主数据新增 `market`、`asset_class`、`taxonomy_type`、`parent_bucket`、`index_code` 等后台字段；审计输出新增 `by_taxonomy_type` 和 `by_parent_bucket`，便于后续按主流机构做法逐步引入行业/主题/策略分层和成分暴露校验。
- 优化 ETF 分类后台可靠性：新增分类详情和审计脚本 `scripts/audit_etf_taxonomy.py`，区分明确指数映射 `benchmark_exact` 和关键词兜底 `benchmark_pattern`；补充深证50、国证2000、中证800、中小100、金融科技、数字经济、消费电子、航空航天、卫星通信、港股汽车、港股创新药等高确定性指数映射；新增巴西、印度、越南、韩国、英国、欧洲、全球等非港股海外 ETF 排除标记。审计显示精确映射从 613 提升到 796，关键词兜底从 378 降到 233，目标权益 ETF 分类覆盖率从 73.79% 提升到 76.85%。
- 将 ETF 数据日完整性调整为严格口径：最新份额接口中出现的目标权益 ETF 必须 100% 同日存在价格和份额数据，当天才可作为全页 ETF 结论数据日；否则自动回退到最近 100% 完整交易日。数据状态行同步移动到分区 tab 下方、总览模块上方，明确约束的是全页 ETF 结论。
- 调整 ETF 净申购占比口径：从“窗口净申购金额 / 当日 ETF 规模”改为“窗口净申购金额 / 窗口期初 ETF 规模”，更接近资金流入强度口径；表头问号同步说明期初规模按窗口起点交易日份额和收盘价估算。
- 增加 TuShare 原始数据文件缓存：`fund_daily`、`fund_share`、`fund_nav` 按日期缓存到 `data/tushare_cache/`；近期数据短 TTL，历史数据长期复用，服务重启后不再重复拉取完整 60 日历史窗口；支持 `CAPITAL_FLOW_DISABLE_FILE_CACHE=1` 绕过缓存排障。
- 调整资金流向观察窗口：从 `1日 / 3日 / 7日 / 30日` 改为 `1日 / 3日 / 5日 / 20日 / 60日`，全部按 ETF 有效交易日统计；5日、20日、60日分别对应近一周、近一月、近一季的主流观察框架，展开曲线同步改为 60 日。
- 强化 ETF 净申购日期口径：价格日和份额日必须硬对齐；最新交易日数据不完整时自动回退到最近完整交易日；完整窗口不足时 API 不输出错配结论；净值仍作为同日优先价格源，缺失时回退同日收盘价估算。页面和 API 增加 ETF 数据状态，展示数据日、价格日、份额日、净值日和回退状态。
- 优化资金流向页面展示：删除页头右侧重复的“资金流向”导航入口；将“策略/因子”统一命名为“策略因子”；分区顺序调整为“总览、宽基、A股行业、港股行业、策略因子”；表格列名缩短为“X日净申购”和“X日净申购占比”；移除总览矩阵竖向分隔线并收窄表格最小宽度。
- 补充运维注意事项：模板或静态资源变更后需要重启 `com.capital-flow.web`，避免后台 Flask 进程继续使用旧模板缓存，导致旧导航与新样式混用。
- 完善项目交接文档：扩展根目录 README，新增 `docs/architecture.md`，重写 `docs/operations.md` 和 `docs/data_sources.md`，补充项目边界、启动验证、LaunchAgent、局域网访问、TuShare 接口、ETF 净申购公式、分类规则、展示阈值和常见差异来源，方便后续新 session 接手。
- 从家庭资产项目拆出为独立资金流向项目。
- 独立服务默认运行在 `5083`，首页即资金流向页面。
- 项目只保留 TuShare 相关配置和资金流向计算，不读取家庭资产数据库。
